<%- include("../parts/header") %>
		<h1>Users</h1>
		<ul>
			<%
				if (locals.users) {
					for (const user of users) {
			%>
			<li>
				<h2><%= user.id %>: <%= user.username %></h2>
				<p>
			</li>
			<%
					}
				}
			%>
		</ul>
<%- include("../parts/footer") %>